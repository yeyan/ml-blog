// import UIkit from '../../node_modules/uikit';
// import Icons from '../../node_modules/uikit/dist/js/uikit-icons';
// 
// // loads the Icon plugin
// UIkit.use(Icons);

// components can be called from the imported UIkit reference
// UIkit.notification('Webpack UIKit 3 bundle loaded!');

//import avatarImageSrc from '../img/yeyan.jpg'
//
//document.addEventListener('DOMContentLoaded', (event) => {
//  const img = document.getElementById("avatar-image")
//  // absolute path
//  img.src = avatarImageSrc.substring(2, avatarImageSrc.length)
//})
